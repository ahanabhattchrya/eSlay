import React from "react";
import { Box, Button, Grid } from "@material-ui/core";

const Dashboard = () => {
	return (
		<div class="dashboard">
			<Grid></Grid>
		</div>
	);
};

export default Dashboard;
